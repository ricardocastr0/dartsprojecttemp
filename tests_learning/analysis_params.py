"""Local defaults for tests_learning analysis only (immutable vs main package config)."""

# Default rolling window for analyze_training_logs.py (override CLI with --rolling).
DEFAULT_ROLLING_WINDOW = 50
